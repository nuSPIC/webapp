You are receiving this email because you are watching the topic "{{ post.topic.name }}" at {{ site_name }}.

The user {{ post.profile }} added a reply to the topic "{{ post.topic.name }}":

{{ post.message|safe }}

http://{{ domain }}{{ post.get_absolute_url }}
